//CODE BY SUDARSHAN RAJKUMAR_FROM_MANIPUR
#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define endl '\n'
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];

        }
        set<int>s;
        for(int val:s){//set is a data structure for unique elements
            s.insert(val);
        }
        for(auto it:s){
            arr[index]=it;
            index++;
        }



        
        
    
    
    return 0;
}