#include<bits/stdc++.h>
using namespace std;
using ll= long long;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
     ll n;cin>>n;
    cout<<9*n<<" "<<8*n<<endl;//Multiplication of a composite no with a no is also a composite no...
    return 0;
}